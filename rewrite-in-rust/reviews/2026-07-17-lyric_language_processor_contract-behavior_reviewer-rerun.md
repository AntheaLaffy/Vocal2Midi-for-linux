# lyric_language_processor_contract - behavior_reviewer rerun

Date: 2026-07-17
Decision: pass

Unit: `lyric_language_processor_contract`
Role: `behavior_reviewer`

## Findings

No behavior parity findings.

Evidence:

- Factory selection, ordering, and unsupported-language text match legacy Python. Python preserves insertion order `zh`, `en`, `ja`, lowercases the lookup key, and raises `ValueError(f"Unsupported language: {language_code}")` while retaining the original rejected code (`inference/LyricFA/tools/language_processors.py:111`, `inference/LyricFA/tools/language_processors.py:119`, `inference/LyricFA/tools/language_processors.py:126`). Rust returns the same supported-language order, lowercases for dispatch, and formats `Unsupported language: <original>` (`rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:36`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:71`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:81`). The fixture covers `zh`, `ZH`, `en`, `ja`, and `ko` (`rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:1`).
- Clean/split/phonetic parity is fixture-backed for English, Chinese, and Japanese. Python defines base clean filtering and whitespace compaction at `LanguageProcessor.clean_text`, with Chinese inheriting the current bracket-adjacent regex artifact from `_CHINESE_CHAR_RANGE` (`inference/LyricFA/tools/language_processors.py:15`, `inference/LyricFA/tools/language_processors.py:28`). Rust now routes Chinese cleaning through `clean_chinese_legacy_regex` before whitespace compaction, and its allowed set matches the legacy artifact's literal `[` plus CJK range behavior (`rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:193`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:324`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:342`). The rerun fixture locks the observed `"a]b[c^d-e\\f中  文" -> "b[c^d-e\\f中 文"` behavior (`rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:5`).
- Chinese split and phonetic flow match the scoped injected-map seam. Python delegates to `zh_split_string` and `ZhG2p.convert_list(...).split(' ')` (`inference/LyricFA/tools/language_processors.py:35`, `inference/LyricFA/tools/language_processors.py:38`); Rust delegates to `zh_g2p::split_string` and preserves empty ASCII-space splitting from the converted string (`rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:197`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:201`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:346`). Fixtures cover injected Mandarin maps and the legacy Chinese empty-input phonetic shape `[""]` (`rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:4`, `rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:6`).
- English behavior matches Python's clean, lowercase split, and identity phonetics. Python filters allowed ASCII punctuation, lowercases before `zh_split_string`, and returns `text_list` as phonetics (`inference/LyricFA/tools/language_processors.py:42`, `inference/LyricFA/tools/language_processors.py:48`, `inference/LyricFA/tools/language_processors.py:51`). Rust mirrors those steps in `EnglishProcessor` (`rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:219`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:234`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:238`). Fixtures cover punctuation removal, lowercase word/digit splitting, apostrophe/hyphen retention, and identity phonetics (`rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:2`, `rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:3`).
- Japanese fallback flow and `build_reference_lyric` match the pyopenjtalk-absent contract. The Python checker forces `JaG2p.pyopenjtalk = None` before importing the processors (`rewrite-in-rust/bootstrap/check_lyric_language_processor_contract.py:17`, `rewrite-in-rust/bootstrap/check_lyric_language_processor_contract.py:20`). Python normalizes whitespace only, splits kana display moras, converts ordinary phonetics with `convert_number=True`, and builds reference tuples with `convert_number=False` (`inference/LyricFA/tools/language_processors.py:62`, `inference/LyricFA/tools/language_processors.py:66`, `inference/LyricFA/tools/language_processors.py:73`, `inference/LyricFA/tools/language_processors.py:78`). Rust follows the same flow through `JapaneseProcessor` and `JaG2p` (`rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:258`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:262`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:270`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:281`). Fixtures cover ordinary kana/romaji conversion, number handling in reference mode, and empty reference input (`rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:8`, `rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:9`, `rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:10`).
- `LyricData` output shape and ordering are covered directly after the prior dependency follow-up. Python declares the dataclass as `text_list`, `phonetic_list`, `raw_text`, and `LyricMatcher.process_lyric_text` returns those values in that order with `raw_text` set to the cleaned text (`inference/LyricFA/tools/language_processors.py:104`, `inference/LyricFA/tools/lyric_matcher.py:37`, `inference/LyricFA/tools/lyric_matcher.py:45`). Rust declares the same public fields and `process_text` / `process_reference_lyric` populate cleaned `raw_text` after list construction (`rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:15`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:143`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:156`). The fixture/checker now exercise a `lyric_data_shape` case, and the Rust fixture test compares the actual `LyricData` returned by `process_text` (`rewrite-in-rust/fixtures/lyric_language_processor_contract.jsonl:7`, `rewrite-in-rust/bootstrap/check_lyric_language_processor_contract.py:126`, `rewrite-in-rust/rust/crates/v2m-core/src/lyric_language.rs:486`).
- Rollback and no-production-wiring constraints remain intact. The manifest keeps this unit `status: reimplemented` and `current_owner: legacy`, not verified/promoted (`rewrite-in-rust/manifest.yaml:1271`, `rewrite-in-rust/manifest.yaml:1273`, `rewrite-in-rust/manifest.yaml:1275`). Records require implementation without production routing and state that rollback is keeping all language processor runtime calls in Python (`rewrite-in-rust/records/0064-implement-lyric-language-processor-contract.md:21`, `rewrite-in-rust/records/0064-implement-lyric-language-processor-contract.md:49`). The bootstrap also states no production caller imports Rust language processor helpers until a promotion record defines the missing runtime decisions (`rewrite-in-rust/bootstrap/lyric_language_processor_contract.md:73`, `rewrite-in-rust/bootstrap/lyric_language_processor_contract.md:148`). A targeted repository search found runtime `ProcessorFactory` / `LyricData` imports still point to Python in `inference/LyricFA/tools/lyric_matcher.py`, while the Rust module is only exposed inside the standalone Rust crate (`inference/LyricFA/tools/lyric_matcher.py:7`, `rewrite-in-rust/rust/crates/v2m-core/src/lib.rs:18`).

## Checks

- `uv run python rewrite-in-rust/bootstrap/check_lyric_language_processor_contract.py`: passed.
- `cargo test --manifest-path rewrite-in-rust/rust/Cargo.toml lyric_language`: passed; 1 `lyric_language` test passed in `v2m-core`, 97 tests filtered out, and 0 selected bridge tests.
- `git diff --check`: passed.
- Static review of manifest, records 0063/0064, dependency/bootstrap artifacts, fixture/checker, Python sources, Rust implementation, and production-search results: no behavior parity blocker found and no production Rust routing detected.

## Residual Risk

This review proves the current fixture-bound, no-bridge behavior surface only. It does not prove bundled Chinese dictionary file IO/packaging, malformed dictionary payload handling, OpenJTalk frontend-backed Japanese behavior, Python-facing bridge error/log mapping, GUI/Web/CLI integration, or production routing. Those remain outside this behavior role and must be handled before any promotion wires Rust into runtime callers.

## Promotion Note

This rerun does not block coordinator update for the `behavior_reviewer` role. The coordinator can record this behavior role as passed, but should not mark the manifest unit verified or promoted from this report alone.
