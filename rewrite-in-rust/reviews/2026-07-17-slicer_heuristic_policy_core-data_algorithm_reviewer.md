# slicer_heuristic_policy_core - data_algorithm_reviewer

Date: 2026-07-17
Decision: pass

## Findings

No findings for the `data_algorithm_reviewer` role.

## Scope Confirmation

- Unit and role reviewed: `slicer_heuristic_policy_core`, `data_algorithm_reviewer`.
- Manifest state: the unit is `reimplemented`, confirmed, legacy-owned, and requires dependency/bootstrap, stage behavior, and data/algorithm reviews (`rewrite-in-rust/manifest.yaml:1064`, `rewrite-in-rust/manifest.yaml:1066`, `rewrite-in-rust/manifest.yaml:1073`).
- Confirmed boundary: record 0049 limits this unit to `heuristic_slice` policy ordering, fixed dependency parameters, long-segment split dispatch, parent offset adjustment, exact-min/short/ultra-short retention, sort-before-merge, and merge-helper handoff; it excludes Slicer internals, RMS/window splitting internals, merge internals, grid search, pitch/RMVPE smart slicing, audio IO, GUI/Web, and model execution (`rewrite-in-rust/records/0049-confirm-slicer-heuristic-policy-boundary.md:21`, `rewrite-in-rust/records/0049-confirm-slicer-heuristic-policy-boundary.md:23`, `rewrite-in-rust/records/0049-confirm-slicer-heuristic-policy-boundary.md:28`).
- Writer/reviewer separation: preserved. I inspected sources, records, fixtures, and ran non-mutating checks; I wrote only this review report and did not edit production code, manifests, fixtures, bootstrap records, dependency records, or Rust modules.

## Data And Algorithm Assessment

- Data representation matches the selected fixture seam. The unit receives dependency-produced `Segment` values over the shared mono/channel-major stereo `Waveform` model, while the dependency record keeps default Slicer, RMS/window internals, and tiny/short merge internals in separate units (`rewrite-in-rust/dependencies/slicer_heuristic_policy_core.yaml:8`, `rewrite-in-rust/dependencies/slicer_heuristic_policy_core.yaml:27`, `rewrite-in-rust/dependencies/slicer_heuristic_policy_core.yaml:30`, `rewrite-in-rust/dependencies/slicer_heuristic_policy_core.yaml:32`). The bootstrap explicitly selects fake `Slicer` and fake sliding-window dependencies for policy proof while leaving real merge helpers in place for observable handoff/order behavior (`rewrite-in-rust/bootstrap/slicer_heuristic_policy_core.md:82`, `rewrite-in-rust/bootstrap/slicer_heuristic_policy_core.md:84`).
- Duration semantics preserve the legacy `len(waveform) / sr` policy quirk. Python computes `seg_dur = len(segment['waveform']) / sr` before deciding whether to split or retain (`inference/API/slicer_api.py:408`, `inference/API/slicer_api.py:409`, `inference/API/slicer_api.py:423`, `inference/API/slicer_api.py:425`). Rust uses `legacy_len_duration_sec`, which divides `Waveform::outer_len()` by the sample rate (`rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:169`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:196`). `outer_len()` is sample count for mono and channel count for stereo, matching Python `len(waveform)` (`rewrite-in-rust/rust/crates/v2m-core/src/slicer_segment.rs:24`). Fixture line 5 proves the stereo long-segment branch uses channel-count duration, not sample-axis duration (`rewrite-in-rust/fixtures/slicer_heuristic_policy_core.jsonl:5`).
- Long-segment split dispatch and offset adjustment match the legacy order. Python calls `_sliding_window_split` with caller min/max, caller split threshold, and fixed `frame_length=2048` / `hop_length=512`, then adds the parent segment offset to each returned subchunk before appending (`inference/API/slicer_api.py:412`, `inference/API/slicer_api.py:414`, `inference/API/slicer_api.py:417`, `inference/API/slicer_api.py:420`). Rust builds the same `SplitRequest`, invokes the injected splitter only for `segment_duration > max_len_sec`, adds `segment.offset`, and appends subchunks (`rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:48`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:166`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:171`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:172`). Fixture line 2 proves arguments and parent-offset addition (`rewrite-in-rust/fixtures/slicer_heuristic_policy_core.jsonl:2`).
- Non-long retention semantics are output-equivalent. Python has separate exact-min/short/ultra-short logging branches, but all three append the segment to `final_chunks` (`inference/API/slicer_api.py:423`, `inference/API/slicer_api.py:425`, `inference/API/slicer_api.py:428`). Rust collapses those output-equivalent branches into a single `else` that clones the segment (`rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:176`). This is acceptable for the data/algorithm role because logging text is explicitly deferred to promotion wiring (`rewrite-in-rust/bootstrap/slicer_heuristic_policy_core.md:108`).
- Offset ordering and merge handoff match for normal finite offsets. Python sorts `final_chunks` by `offset`, then hands the sorted list to `_merge_tiny_chunks`, then `_merge_short_segments` when non-empty (`inference/API/slicer_api.py:432`, `inference/API/slicer_api.py:433`, `inference/API/slicer_api.py:436`, `inference/API/slicer_api.py:442`). Rust performs the same order with stable `sort_by`, `merge_tiny_chunks`, and guarded `merge_short_segments` (`rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:181`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:182`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:184`). Fixture lines 3 and 4 prove sort-before-merge and short-merge handoff (`rewrite-in-rust/fixtures/slicer_heuristic_policy_core.jsonl:3`, `rewrite-in-rust/fixtures/slicer_heuristic_policy_core.jsonl:4`).
- Pre-slicer configuration is preserved. Python constructs `Slicer` with caller sample rate, caller silence threshold, caller `min_silence_len_ms`, fixed `min_interval=200`, and fixed `max_sil_kept=100` (`inference/API/slicer_api.py:396`, `inference/API/slicer_api.py:397`, `inference/API/slicer_api.py:399`, `inference/API/slicer_api.py:400`). Rust exposes the same `PreSlicerParams` and the composing helper passes default hop-size `20.0` to `Slicer::new`, matching the legacy constructor default (`rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:36`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:122`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:128`, `inference/slicer/slicer2.py:47`). Fixture line 1 and the checker fake prove the observed constructor parameters (`rewrite-in-rust/fixtures/slicer_heuristic_policy_core.jsonl:1`, `rewrite-in-rust/bootstrap/check_slicer_heuristic_policy_core.py:86`).
- Complexity is acceptable for the compatibility unit. The policy layer is linear over pre-sliced segments plus one sort over retained/split chunks, then delegates copy-heavy tiny/short merging to the accepted merge helper unit. There is no new nested search, scoring loop, RMS pass, or model/audio work in this unit (`rewrite-in-rust/records/0049-confirm-slicer-heuristic-policy-boundary.md:28`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:169`, `rewrite-in-rust/rust/crates/v2m-core/src/slicer_heuristic.rs:181`).

## Checks

- `uv run python rewrite-in-rust/bootstrap/check_slicer_heuristic_policy_core.py`: pass, exit 0 with no output.
- `cargo test --manifest-path rewrite-in-rust/rust/Cargo.toml slicer_heuristic`: pass; 2 `slicer_heuristic` tests passed, 83 `v2m_core` tests and 5 `v2m_quant_bridge` tests were filtered out.
- `cargo test --manifest-path rewrite-in-rust/rust/Cargo.toml slicer_segment`: pass; 2 `slicer_segment` tests passed, 84 `v2m_core` tests and 5 `v2m_quant_bridge` tests were filtered out.
- `cargo test --manifest-path rewrite-in-rust/rust/Cargo.toml slicer_window`: pass; 2 `slicer_window` tests passed, 84 `v2m_core` tests and 5 `v2m_quant_bridge` tests were filtered out.
- `cargo test --manifest-path rewrite-in-rust/rust/Cargo.toml slicer_default`: pass; 3 `slicer_default` tests passed, 83 `v2m_core` tests and 5 `v2m_quant_bridge` tests were filtered out.
- `uv run python rewrite-in-rust/bootstrap/check_slicer_segment_merge_core.py`: pass, exit 0 with no output.
- `uv run python rewrite-in-rust/bootstrap/check_slicer_rms_db_window_split_core.py`: pass, exit 0 with no output.
- `uv run python rewrite-in-rust/bootstrap/check_slicer_rms_and_default_core.py`: pass, exit 0 with no output.
- `git diff --check`: pass, exit 0 with no output.

## Residual Risk

- This review is fixture-bound over normal finite offsets and positive sample rates. Non-finite offsets, signed-zero ordering, negative offsets, and non-positive sample rates are not pinned here; production promotion should validate waveform and segment payloads before Rust helpers become runtime-owned.
- The data/algorithm decision relies on already accepted dependency units for default slicing, RMS/window splitting, and segment merging. Their promotion-time residual risks still apply, including waveform shape validation and the RMS/window near-threshold float policy recorded when the window-split gate closed (`rewrite-in-rust/records/0048-close-slicer-rms-db-window-split-gate.md:32`).
- The fixture test proves policy orchestration with fake `Slicer` and fake sliding-window dependencies, not end-to-end production slicing over decoded audio. That is consistent with the confirmed boundary, but a future promotion record still needs dependency wiring, logging text, payload validation, and error mapping (`rewrite-in-rust/bootstrap/slicer_heuristic_policy_core.md:108`).

## Promotion Note

This `data_algorithm_reviewer` role does not block coordinator state update for the unit's data/algorithm evidence. It does not by itself mark the unit fully verified; the coordinator still needs the other required review evidence for `slicer_heuristic_policy_core` before promotion.
