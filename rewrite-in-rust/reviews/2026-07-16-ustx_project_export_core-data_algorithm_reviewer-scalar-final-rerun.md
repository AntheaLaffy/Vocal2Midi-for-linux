# ustx_project_export_core - data_algorithm_reviewer

Date: 2026-07-16
Decision: pass-with-followups

## Findings

- Severity: low
- Location: rewrite-in-rust/fixtures/ustx_project_export_core.jsonl:2
- Issue: No blocking data-shape mismatch remains, but full USTX golden fixture coverage is still thinner than the scalar helper coverage. The public fixture now routes an exponent-looking project stem and a binary-looking lyric through `render_ustx_project`, while octal, hex, and sexagesimal-looking strings are covered at helper level rather than as separate full project fixtures.
- Evidence: Legacy accepts arbitrary project names from `filepath.stem` and lyrics from `note.lyric or "a"` at `inference/API/ustx_api.py:384`, `inference/API/ustx_api.py:414`, and `inference/API/ustx_api.py:447`, then serializes with PyYAML at `inference/API/ustx_api.py:460`. Rust routes project names and lyrics through the same scalar renderer at `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:70`, `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:90`, and `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:149`. The current fixture pins `stem: "1.0e-07"` and lyric `"0b101"` at `rewrite-in-rust/fixtures/ustx_project_export_core.jsonl:2`; the golden YAML keeps those values quoted strings at `rewrite-in-rust/fixtures/ustx_project_export_core/edge_project.ustx:1`, `rewrite-in-rust/fixtures/ustx_project_export_core/edge_project.ustx:242`, and `rewrite-in-rust/fixtures/ustx_project_export_core/edge_project.ustx:274`. Rust helper tests cover exponent, binary, octal, hex, and sexagesimal resolver forms at `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:899`.
- Required fix: Before runtime promotion, consider adding one compact full-render fixture or public render test that sends octal, hex, and sexagesimal-looking lyrics through `render_ustx_project`. This is not a blocker for this data/algorithm review because all accepted dynamic string fields share the same `yaml_scalar` path and the scalar resolver tests pass.

## Checked Scope

- The prior exponent-string scalar blocker is closed. `yaml_scalar` now calls the resolver-sensitive number/date/time detectors before returning a plain scalar at `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:219` through `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:237`. The number detector covers binary, hex, legacy octal, decimal, decimal-exponent float, and sexagesimal forms at `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:343` through `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:515`.
- Numeric tempo exponent formatting remains closed. Rust normalizes finite exponent floats in `python_float_repr` at `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:193` and uses that text for both root `bpm` and `tempos[0].bpm` at `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:67`, `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:80`, and `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:84`. The empty fixture pins PyYAML-compatible `1.0e-07` output at `rewrite-in-rust/fixtures/ustx_project_export_core/empty_project.ustx:7` and `rewrite-in-rust/fixtures/ustx_project_export_core/empty_project.ustx:227`.
- Tick conversion, sorting/filtering, half-even rounding, tone clamp, and voice duration remain aligned for the narrowed `rmvpe_result=None` surface. Python filtering, sorting, tick conversion, min duration, tone clamp, lyric fallback, and voice duration are at `inference/API/ustx_api.py:30`, `inference/API/ustx_api.py:370`, `inference/API/ustx_api.py:374`, `inference/API/ustx_api.py:376`, `inference/API/ustx_api.py:377`, `inference/API/ustx_api.py:384`, and `inference/API/ustx_api.py:446`. Rust mirrors those with finite filtering, stable sorting, half-even tick conversion, min duration, tone clamp, lyric fallback, and max-end voice duration at `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:54`, `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:126`, `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:128`, `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:132`, `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:160`, `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:164`, `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:174`, and `rewrite-in-rust/rust/crates/v2m-core/src/ustx_project.rs:65`.
- The unit stays inside the confirmed boundary: project/note YAML for `save_ustx(..., rmvpe_result=None)`, with pitch curves and RMVPE kept out of scope by `rewrite-in-rust/records/0042-confirm-ustx-project-export-boundary.md:18` and `rewrite-in-rust/records/0042-confirm-ustx-project-export-boundary.md:33`.
- Complexity remains acceptable. Both paths do one finite-note filter pass, one stable sort, one render pass over valid notes, and fixed-size project skeleton emission.

## Checks

- `uv run python rewrite-in-rust/bootstrap/check_ustx_project_export_core.py`: pass.
- `cargo test --manifest-path rewrite-in-rust/rust/Cargo.toml ustx_project`: pass; 6 tests passed.
- `uv run python -c "import pathlib, yaml; edge=pathlib.Path('rewrite-in-rust/fixtures/ustx_project_export_core/edge_project.ustx'); data=yaml.safe_load(edge.read_text(encoding='utf8')); assert data['name']=='1.0e-07' and isinstance(data['name'], str); vp=data['voice_parts'][0]; assert vp['name']=='1.0e-07' and isinstance(vp['name'], str); lyrics=[n['lyric'] for n in vp['notes']]; assert lyrics[1]=='0b101' and isinstance(lyrics[1], str); vals=['1.0e-07','1.0e+20','-1.0e-07','+1.0e+20','0b101','-0b101','077','0x10','1:20','1:20.5']; dumped=yaml.safe_dump({'values': vals}, allow_unicode=True, sort_keys=False); loaded=yaml.safe_load(dumped)['values']; assert loaded == vals, (dumped, loaded); print(dumped, end=''); print('loaded types:', [type(v).__name__ for v in loaded])"`: pass; confirmed the fixture and PyYAML probe keep exponent, binary, octal, hex, and sexagesimal-looking strings as strings.
- `uv run python -c "import yaml; vals=['1.0e7','1.0e-7','1.0e+7','1e-7','1e+7','.5','+.5','-.5','1_000','0_7','00']; dumped=yaml.safe_dump({'values': vals}, allow_unicode=True, sort_keys=False); print(dumped, end=''); print('loads:', [(v, type(yaml.safe_load('x: '+v+'\\n')['x']).__name__) for v in vals])"`: pass; confirmed unsigned exponent strings such as `1.0e7` and `1e-7` are not resolver-active under PyYAML, while decimal/exponent, underscore, and legacy octal forms that are resolver-active are covered by Rust's detector.

## Residual Risk

The fixture table is still small. It does not independently pin every non-finite position of the `(onset, offset, pitch)` tuple, negative-time rounding, same-onset stable ordering, very large tick overflow, non-finite tempo errors, or every PyYAML scalar style. These are promotion hardening follow-ups, not current data-shape blockers for the reviewed scalar fix.

## Promotion Note

This data/algorithm role no longer blocks promotion of `ustx_project_export_core`. The coordinator should still require the other required review roles to pass before changing the manifest state from `reimplemented` to `verified`.
