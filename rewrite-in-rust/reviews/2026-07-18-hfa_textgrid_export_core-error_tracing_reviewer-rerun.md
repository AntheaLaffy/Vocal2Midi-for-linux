# hfa_textgrid_export_core - error_tracing_reviewer rerun

Date: 2026-07-18
Decision: pass
Unit: `hfa_textgrid_export_core`
Role: `error_tracing_reviewer`
Rerun after: `rewrite-in-rust/records/0094-fix-hfa-textgrid-double-slash-paths.md`

## Findings

No findings.

- Severity: none
- Location: `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:45`
- Issue: The post-0094 TextGrid planner keeps an acceptable error/tracing surface. It exposes structured Python-compatible errors, carries partial planned side effects on failures, preserves diagnosable path and interval failure ordering, adds no logging or filesystem side effects, and does not introduce public-input panic paths in the reviewed surface.
- Evidence: `HfaTextGridExportError` stores the legacy exception type plus message and projects both through accessors/`Display` (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:45`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:52`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:64`), while `HfaTextGridExportFailure` carries the cloned partial plan (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:72`). The public planner clones completed side effects before returning interval, path, or render errors (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:106`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:113`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:121`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:130`). Interval and serialization projections use legacy `ValueError`/`IndexError` messages (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:179`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:199`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:302`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:502`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:509`) matching the Python/textgrid reference (`third_party/sources/textgrid-1.6.1/textgrid/textgrid.py:186`, `third_party/sources/textgrid-1.6.1/textgrid/textgrid.py:205`, `third_party/sources/textgrid-1.6.1/textgrid/textgrid.py:453`, `third_party/sources/textgrid-1.6.1/textgrid/textgrid.py:751`). Fixture rows cover zero-duration and overlap failures before side effects, an out-of-range failure after a prior planned write, and empty wav-path errors before side effects (`rewrite-in-rust/fixtures/hfa_textgrid_export_core.jsonl:11`, `rewrite-in-rust/fixtures/hfa_textgrid_export_core.jsonl:12`, `rewrite-in-rust/fixtures/hfa_textgrid_export_core.jsonl:13`, `rewrite-in-rust/fixtures/hfa_textgrid_export_core.jsonl:14`).
- Evidence: Record 0094's double-slash fix is limited to path projection and keeps production callers unchanged (`rewrite-in-rust/records/0094-fix-hfa-textgrid-double-slash-paths.md:16`, `rewrite-in-rust/records/0094-fix-hfa-textgrid-double-slash-paths.md:23`). The helper preserves exactly-two-leading-slash paths while still dropping lexical `.` components (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:394`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:408`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:418`) and the same normalized helper feeds empty-name error display (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:381`, `rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:445`). The 18-row fixture set now locks `//out`, `//a/b/song.wav`, and `//song.wav` path plans (`rewrite-in-rust/fixtures/hfa_textgrid_export_core.jsonl:16`, `rewrite-in-rust/fixtures/hfa_textgrid_export_core.jsonl:17`, `rewrite-in-rust/fixtures/hfa_textgrid_export_core.jsonl:18`). A targeted public Rust probe matched Python for empty-name display on `""`, `"."`, `"/"`, `"//"`, `"///"`, and `"//."`, with `ValueError` messages such as `PosixPath('//') has an empty name` and empty partial plans before any directory/file plan.
- Evidence: The module contract keeps directory creation, writes, status printing, artifact copying, dispatch, and production routing out of Rust (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:1`), consistent with the dependency record's kept-legacy boundary (`rewrite-in-rust/dependencies/hfa_textgrid_export_core.yaml:35`) and record 0093's no-production-route statement (`rewrite-in-rust/records/0093-implement-hfa-textgrid-export-core.md:12`). The legacy Python owner still performs status printing, mkdir, and writes (`inference/HubertFA/tools/export_tool.py:11`), while API artifact copying remains separate caller behavior (`inference/API/hfa_api.py:145`). Static scans found no Rust filesystem, stdout/stderr, logging, tracing, or debug-macro side effects in the module. The panic scan found one non-test `expect("Rust float debug exponent is an integer")` in Python-float text rendering (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:494`); it is guarded by Rust `f64` debug-format structure rather than parsed public text. Remaining `panic!`/`unwrap` hits are under `#[cfg(test)]` (`rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs:516`).
- Required fix: None.

## Checks

- `PYTHONDONTWRITEBYTECODE=1 uv run python --version`: passed; Python 3.12.13.
- `PYTHONDONTWRITEBYTECODE=1 uv run python rewrite-in-rust/bootstrap/check_hfa_textgrid_export_core.py`: passed; validated 18 Python-generated fixtures through the real monkeypatched `Exporter.save_textgrids`.
- `CARGO_TARGET_DIR=/tmp/v2m-hfa-textgrid-error-review-target cargo test --manifest-path rewrite-in-rust/rust/Cargo.toml --locked hfa_textgrid_export_core -- --nocapture`: passed; 1 focused Rust fixture-parity test passed, 116 `v2m-core` tests and 5 `v2m-quant-bridge` tests filtered out.
- `rg -n "std::fs|File::|create_dir|write_all|println!|eprintln!|dbg!|log::|tracing::" rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs`: no matches.
- `rg -n "panic!|todo!|unimplemented!|unwrap\\(|expect\\(" rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs`: inspected; only the guarded float-format `expect` is outside tests, and all other hits are test fixture decoding/assertion code.
- `rg -n "hfa_textgrid_export|plan_textgrid_export|HfaTextGrid|save_textgrids|TextGrid" rewrite-in-rust/rust inference application web_server.py web_task_manager.py scripts tests -g '!target'`: inspected; production Python still routes through legacy `Exporter.save_textgrids`, with the Rust planner exposed only in the independent `v2m-core` workspace.
- `git diff --check -- rewrite-in-rust/rust/crates/v2m-core/src/hfa_textgrid_export.rs rewrite-in-rust/bootstrap/check_hfa_textgrid_export_core.py rewrite-in-rust/fixtures/hfa_textgrid_export_core.jsonl rewrite-in-rust/dependencies/hfa_textgrid_export_core.yaml rewrite-in-rust/bootstrap/hfa_textgrid_export_core.md rewrite-in-rust/records/0093-implement-hfa-textgrid-export-core.md rewrite-in-rust/records/0094-fix-hfa-textgrid-double-slash-paths.md`: passed.
- Targeted Python and public Rust path-display probes for `""`, `"."`, `"/"`, `"//"`, `"///"`, and `"//."`: passed; Rust exception type/message projection and empty partial plans matched Python's `pathlib` display behavior, including the exactly-two-slash root.

## Residual Risk

This rerun covers only the `error_tracing_reviewer` role. It does not re-review behavior parity beyond error projection and diagnosability evidence, dependency/bootstrap sufficiency, data/algorithm choices, Rust style, architecture, or product ergonomics. Real filesystem IO errors, status output, export dispatch, API artifact copying, and execution of planned side effects remain intentionally legacy-owned. Non-UTF-8 Rust `Path` display remains a low residual risk because the Python compatibility source is `pathlib` string-path behavior, not byte-path inputs.

## Coordinator Readiness

This rerun is ready for coordinator consumption as a passing `error_tracing_reviewer` gate after record 0094. It does not block promotion on error/tracing grounds. The coordinator should not mark the whole unit `verified` from this role alone; `hfa_textgrid_export_core` still needs the other required role outcomes, including the post-0094 behavior rerun, before any manifest state update to `verified`.
